services:
  - type: web
    name: saas-school-management-v2
    env: node
    plan: free
    nodeVersion: 18
    buildCommand: "npm run build && cd backend && npm install"
    startCommand: "npm start"
    envVars:
      - key: NODE_ENV
        value: "production"
      - key: PORT
        value: "10000"